version: '3.8'

services:

  redis:
    image: redis:alpine
    container_name: redis
    volumes:
      - ./redis_data:/data
    ports:
      - "6379:6379"
    command: redis-server --appendonly yes
    networks:
      - dev_vkr

networks:
  dev_vkr:
    name: dev_vkr
    driver: bridge